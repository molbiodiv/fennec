'use strict';

$('document').ready(function () {
    var table = React.createElement(
        'h1',
        null,
        'Test'
    );
    ReactDOM.render(table, document.getElementById('react-table-test'));
});