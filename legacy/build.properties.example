prefix = 
config_dir = ${prefix}/etc/fennec-web
www_root = ${prefix}/var/www/html
share_path = ${prefix}/share/fennec-web
var_path = ${prefix}/var/fennec-web
www_user = www-data
www_group = www-data
www_mode = 0777

srcdir = ${project.basedir}/src
builddir = /tmp/build/fennec-web

chado_db_host = CHADO
chado_db_name = fennec
chado_db_port = 5432
chado_db_username = fennec
chado_db_password = fennec