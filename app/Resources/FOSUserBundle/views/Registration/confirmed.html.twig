{% extends "@FOSUser/layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
    <h1 class="page-header" id="page-title">{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</h1>
    <h3>And now?</h3>
    <p style="font-size: 16px">If you use FENNEC for the first time and you want to learn the main features follow the user manual <a href="https://fennec.readthedocs.io/en/latest/user.html">here</a>.<p/>
    <p>No idea what to do next? Explore FENNEC's <a href={{ path('trait_search', {'dbversion': dbversion  }) }}>trait database</a> and <a href="{{ path('organism_search', {'dbversion': dbversion}) }}">organism database</a>.</p>
    <p>You already have a community data table? What are you waiting for? Upload your data <a href={{ path('project_overview', {'dbversion': dbversion  }) }}>here</a></p>

{% endblock fos_user_content %}
