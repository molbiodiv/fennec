/**
 * Creates an object of type Biom for showing the projects data using data tables
 * @constructor
 * @param {Object} biom - JSON object containing the biom file of a project 
 * @example 
 * // initializes a Biom object 
 * var biom = new Biom(biom)
 */

